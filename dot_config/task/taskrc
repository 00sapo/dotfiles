# [Created by task 2.6.2 6/9/2024 15:53:38]
# Taskwarrior program configuration file.
# For more documentation, see https://taskwarrior.org or try 'man task', 'man task-color',
# 'man task-sync' or 'man taskrc'

# Here is an example of entries that use the default, override and blank values
#   variable=foo   -- By specifying a value, this overrides the default
#   variable=      -- By specifying no value, this means no default
#   #variable=foo  -- By commenting out the line, or deleting it, this uses the default

# You can also refence environment variables:
#   variable=$HOME/task
#   variable=$VALUE

# Use the command 'task show' to see all defaults and overrides

# Files
data.location=/home/federico/.task

# To use the default location of the XDG directories,
# move this configuration file from ~/.taskrc to ~/.config/task/taskrc and uncomment below

#data.location=~/.local/share/task
#hooks.location=~/.config/task/hooks

# Color theme (uncomment one to use)
#include light-16.theme
#include light-256.theme
#include dark-16.theme
#include dark-256.theme
#include dark-red-256.theme
#include dark-green-256.theme
#include dark-blue-256.theme
#include dark-violets-256.theme
#include dark-yellow-green.theme
#include dark-gray-256.theme
#include dark-gray-blue-256.theme
#include solarized-dark-256.theme
#include solarized-light-256.theme
#include no-color.theme

include .taskrc_pass

news.version=2.6.0

urgency.due.coefficient=12
urgency.user.project.Laudare.coefficient=+2
urgency.user.tag.review.coefficient=-1
uda.reviewed.type=date
uda.reviewed.label=Reviewed
report._reviewed.description=Tasksh review report.  Adjust the filter to your needs.
report._reviewed.columns=uuid
report._reviewed.sort=reviewed+,modified+
report._reviewed.filter=( reviewed.none: or reviewed.before:now-6days ) and ( +PENDING or +WAITING )
tasksh.autoclear=1

context.MSCA.read=pro:MSCA
context.MSCA.write=pro:MSCA

uda.weight.type=string
uda.weight.label=Weight
uda.weight.values=L,LM,M,MW,W
uda.weight.default=M
urgency.uda.weight.L.coefficient=-2
urgency.uda.weight.LM.coefficient=-1
urgency.uda.weight.M.coefficient=0
urgency.uda.weight.MW.coefficient=+1
urgency.uda.weight.W.coefficient=+3

uda.duehard.type=date
uda.duehard.label=Due Hard
urgency.uda.duehard.coefficient=1000

report.next.labels=ID,Active,Age,Deps,P,Project,Tag,Recur,S,Due,Due Hard,Until,Description,Weight,Urg
report.next.columns=id,start.age,entry.age,depends,priority,project,tags,recur,scheduled.countdown,due.relative,duehard.relative,until.remaining,description,weight,urgency
