this_path=$(realpath "$0")
if command -v devbox >/dev/null; then
	devbox global run nvim
else
	for c in $(which -a "nvim"); do
    if test "$(realpath $c)" != "$this_path"; then
			$c
      exit
		fi
	done
  echo "Command not found: nvim"
fi
